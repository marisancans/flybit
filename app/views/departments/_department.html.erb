<% index = department_counter + 1 %>
<div class="col-lg-3 col-md-4 col-xs-6 thumb">
  <div class="thumbnail">
  	<%= link_to categories_path(selected: "#{index}") do %>
	    <%= image_tag "#{@images[department_counter]}" %>
	    <div class="text-under-thumbnail">
	    	<h4><%= department.name %></h4>
	    </div>
	  <% end %>
  </div>
</div>