$('#line-item-row-id-<%= @item %>').remove();
$('#line-item-hr-id-<%= @item %>').remove();

if ($('[id^="line-item-row-id-"').length) {
	$('[id^="litp-id-"]').each(function() {
	   sum += parseFloat($(this).text().slice(3));
		});
	sum_total = sum.toFixed(2);
	$('#line-items-total-sum').text("Total: €" + sum_total).effect( "highlight", {color: 'green'}, 2500 );
	} else {
		location.reload();
}
